kind: Service
apiVersion: v1
metadata:
  name: incident-log-webapi
spec:
  selector:
    pod: incident-log-webapi-label
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: webapi-port